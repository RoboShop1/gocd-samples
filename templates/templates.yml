format_version: 10

templates:
  - name: simple-template
    parameters:
      - name: message
    stages:
      - name: build
        jobs:
          - name: build-job
            tasks:
              - exec:
                  command: echo
                  arguments: